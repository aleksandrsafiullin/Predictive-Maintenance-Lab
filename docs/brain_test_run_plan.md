# Brain test run revision

The user's latest requirements supersede the earlier pasted brief where they conflict.

1. **Audit and design:** trace memory semantics and validation quality; retain real MaleCNS recurrent weights. Completed: old checkpoint uses 20-step reset windows and has severe validation error.
2. **New model:** preserve existing checkpoints; train a new connected 1,000-neuron graph with curated coordinates for every computing neuron. Use chronological persistent states, equal bearing weights and regularization selected inside training bearings.
3. **Failure interval:** select the estimator on training-bearing cross-validation before validation calibration. Report empirical interval, coverage and early/late behavior. The test split was viewed in previous iterations, so final results are exploratory reused-holdout results, not a fresh blind assessment. Future measurements and endpoint labels never enter online inference. Do not force confidence to narrow.
4. **Operational interface:** one test-run screen with Start, Pause, Step and Reset. Show recognizable anatomical brain context and all computing neurons. Synchronize current sensor, neuron processing and forecast interval.
5. **Review and verification:** independent code review, causal and state-equivalence tests, full regression suite, real-source training and browser checks. Verify every model neuron has a rendered position; check interval toggles and rewind determinism.

Ownership: forecasting/training agent; brain-rendering agent; operational-screen agent; root integration, runtime trace, chart, resource scheduling and final verification. Heavy source loading and training run sequentially to avoid memory pressure on this Mac.

Completed 2026-09-14. Final model: `bearings_fly_connectome_reservoir_20260914_122633_a23eba`. All 1,000 computing neurons have anatomical coordinates; continuous inference, calibrated empirical intervals and the single operational screen are integrated. Independent reviews, 309 regression tests and desktop/mobile runtime verification completed. See `output/brain-revision/verification.md` for evidence and accuracy limitations.
